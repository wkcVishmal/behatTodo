default:
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost:8000
      sessions:
        default:
          goutte: ~

  suites:
    default:
      contexts:
        - Behat\MinkExtension\Context\MinkContext